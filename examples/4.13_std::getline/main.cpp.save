#include <iostream>
#include <string>

int main()
{
    std::cout << "Pick 1 or 2: ";
    
    int choice{};
    stc::cin >> choice;
    
    std::cout << "Now enter your name please: ";

    std::string name{};
    std::getline(std::cin >> std::ws, name);

    return 0;
}
