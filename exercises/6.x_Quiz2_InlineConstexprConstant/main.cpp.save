#include <iostream>
#include "constant.h"

int main()
{
    std::cout << "How many students are in yout class? ";
    
    int students{};
    std::cin >> students;
    
    if (students > constants::max_class_size)
        std::cout << "There are too many students in your class";
    else
        std::cout << "This"
    return 0;
}
